name: roku-analysis

on:
  push:
    branches:
      - master

jobs:
  static:
    # don't run job on forks
    if: github.repository == 'cewert/jellyrock'
    uses: ./.github/workflows/_roku-analysis.yml
